name: Actualitzar fonts de Sóller Ara

on:
  workflow_dispatch:
  schedule:
    - cron: "7 * * * *"
  push:
    branches:
      - main
    paths:
      - "sources.json"
      - "scripts/update_sources.py"
      - "social_sources.json"
      - ".github/workflows/update-sources.yml"

permissions:
  contents: write

concurrency:
  group: "update-sources"
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configurar Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Actualitzar fonts
        env:
          META_ACCESS_TOKEN: ${{ secrets.META_ACCESS_TOKEN }}
          META_IG_USER_ID: ${{ secrets.META_IG_USER_ID }}
          META_GRAPH_VERSION: "v26.0"
        run: python scripts/update_sources.py

      - name: Guardar dades actualitzades
        run: |
          if git diff --quiet -- data/posts.json data/posts.js; then
            echo "No hi ha canvis a les dades"
            exit 0
          fi
          git config user.name "soller-ara-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/posts.json data/posts.js
          git commit -m "data: actualitzar fonts automàticament"
          git pull --rebase origin main
          git push
